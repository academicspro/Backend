1. fetch teacher tabel datad while getting all teacher
2. fetch school id from user request body automatically
3. get profile automatically hits when frontend loaded set it to be check onece // Refresh Token Issue
4.  After Feature approved update pending state in admin dashboard 
5. create a list of school in we can update thier existing permission


const { redirect } =
typeof window === "undefined" ? { redirect: "" } : qs.parse(window.location.search);